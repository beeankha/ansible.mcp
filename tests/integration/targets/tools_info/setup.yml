---
- hosts: localhost
  gather_facts: true

  vars:
    ansible_mcp_hosts:
      - name: github
        server_name: github
        bearer_token: "{{ github_mcp_pat }}"
        manifest_path: "{{ playbook_dir }}/manifest.json"

  roles:
    - role: generate_inventory
